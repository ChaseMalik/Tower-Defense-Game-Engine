/**
 * 
 */
/**
 * @author Greg Lyons
 * 
 * The guiContainers package has the containers that are used in the Game Player GUI.
 * There are core containers and secondary containers.
 * New containers are added to the secondary containers subpackage corresponding properties file are added to properties.guiContainers
 * 
 * These containers hold GUI items and make it easier to manage the sizing of components within the overall GUI interface
 *
 */
package gamePlayer.guiContainers;